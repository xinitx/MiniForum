server:
  port: 8200
spring:
  application:
    name: postingService
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 1000MB
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos服务地址
      config:
        file-extension: yaml # 文件后缀名
